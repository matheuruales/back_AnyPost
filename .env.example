# Copia este archivo a ".env" y rellena los valores reales antes de ejecutar la aplicación.
# Nunca subas tu archivo .env al repositorio.
# IMPORTANTE: escribe los valores sin comillas ni espacios extra (por ejemplo OPENAI_API_KEY=sk-1234).

N8N_WEBHOOK_URL=

# Database (PostgreSQL - Railway)
# Only these three variables are required when deploying on Railway.
# Format: jdbc:postgresql://host:port/database
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=
# Optional: defaults to org.postgresql.Driver for PostgreSQL
# SPRING_DATASOURCE_DRIVER=org.postgresql.Driver

# Credenciales del servicio de video Blotato
BLOTATO_API_KEY=
BLOTATO_API_BASE_URL=
BLOTATO_API_TEMPLATE_ID=

# Azure Blob Storage
AZURE_BLOB_CONNECTION_STRING=
AZURE_BLOB_CONTAINER=

# OpenAI
OPENAI_API_KEY=
OPENAI_API_BASE_URL=https://api.openai.com/v1
OPENAI_IMAGE_MODEL=dall-e-3

# Google Mail (para recuperación de contraseña)
GOOGLE_MAIL_USERNAME=
GOOGLE_MAIL_APP_PASSWORD=
PASSWORD_RESET_CODE_TTL_MINUTES=15
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587

# Frontend origins allowed (comma-separated). Example supports local dev and deployed Vercel domains.
# e.g. CORS_ALLOWED_ORIGINS=http://localhost:*,http://127.0.0.1:*,https://*.vercel.app,https://app.anypost.com
CORS_ALLOWED_ORIGINS=
CORS_ALLOW_CREDENTIALS=
